import DebugFirestore from '@/components/DebugFirestore';

export default function Dashboard() {
    return (
        <div>
            <h1>Dashboard</h1>
            <DebugFirestore />
        </div>
    );
}
